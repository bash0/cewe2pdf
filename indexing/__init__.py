"""Create optional indexes from marked text in generated CEWE albums.

``indexing.albumindex`` owns configuration, collection, rendering and merging
of the index outputs; the normal album pipeline only supplies marked text.
"""
