[DEFAULT]
cewe_folder = tests/
hpsFolder = tests/hps

# when using "outer edge" page numbers in a single page output, they
# can be repositioned to be on the right or left edge for all pages.
# Valid values are right, left, original. The default is original
# singlePageNumberPosition = right
